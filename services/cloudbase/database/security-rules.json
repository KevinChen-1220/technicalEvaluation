{
  "schemaVersion": 1,
  "rules": {
    "generation_jobs": {
      "read": "doc._openid == auth.openid",
      "write": false
    },
    "assessments": {
      "read": "doc._openid == auth.openid",
      "write": false
    },
    "user_settings": {
      "read": "doc._openid == auth.openid",
      "write": "doc._openid == auth.openid"
    }
  }
}
