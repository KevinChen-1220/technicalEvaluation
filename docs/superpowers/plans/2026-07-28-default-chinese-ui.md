# 默认中文界面实施计划

1. 建立本地化模块测试，先验证缺失实现导致测试失败。
2. 实现简体中文文案、格式化和错误消息转换函数。
3. 替换 `App.tsx` 中所有应用框架文案，保留模型生成内容的原始语言。
4. 用测试驱动方式将内置示例试卷改为中文。
5. 更新 Web 页面、Manifest 和应用描述，并同步元数据校验脚本。
6. 运行完整测试、类型检查、静态构建及资源校验。
7. 启动本地 Web 页面，在移动端视口覆盖主要流程做视觉和控制台检查。
8. 独立代码审查后修复问题，合并到 `main` 并推送 GitHub。
